---
import Animate from "../../ui/Animate.astro";
import { texts } from "../../../content/texts";

const { comparativa } = texts;
---

<section class="comparativa-hero">
  <div class="container">
    <Animate animation="fade-up">
      <span class="comparativa-hero__badge">{comparativa.hero.badge}</span>
      <h1 class="comparativa-hero__title">{comparativa.hero.title}</h1>
      <p class="comparativa-hero__description">{comparativa.hero.description}</p>
    </Animate>
  </div>
</section>

<style>
  .comparativa-hero {
    background-color: var(--color-primary);
    padding-block: var(--space-16);
    color: white;
  }

  .comparativa-hero__badge {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    background-color: rgb(255 255 255 / 0.15);
    font-size: var(--text-xs);
    font-weight: 700;
    border-radius: 9999px;
    letter-spacing: 0.1em;
    margin-bottom: var(--space-4);
  }

  .comparativa-hero__title {
    font-size: var(--text-4xl);
    text-transform: uppercase;
    font-weight: 900;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: var(--space-4);
  }

  .comparativa-hero__description {
    font-size: var(--text-lg);
    opacity: 0.8;
    max-width: 40rem;
    line-height: 1.6;
  }

  @media (min-width: 768px) {
    .comparativa-hero__title {
      font-size: var(--text-5xl);
    }
  }
</style>